{
  "name": "futures-trading-assistant-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Tauri desktop shell that packages the Futures Trading Assistant into cross-platform installers.",
  "scripts": {
    "backend": "python scripts/build_backend.py",
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
